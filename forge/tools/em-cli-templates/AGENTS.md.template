> **Auto-generated by `em-cli render-gateway` from `forge.config.yml` — DO NOT EDIT BY HAND.**
> Re-run `python3 forge/tools/em-cli.py render-gateway --force` to refresh after editing `forge.config.yml`.

# AGENTS.md — {{PRODUCT_NAME}}

This repository is governed by the **em-development-framework** — a deterministic 6-step ticket lifecycle (`enrich-us` → `plan` → `develop` → `verify` → `commit` → `update-docs`) backed by a LangGraph engine. AI coding agents working in this repo follow that lifecycle as the canonical interaction protocol.

This file is the universal entry point read by Cursor, Codex, Claude Code, GitHub Copilot Workspace, Windsurf, and the GitHub coding agent.

## Project identity

- **Product**: `{{PRODUCT_NAME}}`
- **Stack**: `{{STACK_LANGUAGE}}` / `{{STACK_FRAMEWORK}}` / `{{STACK_VERSION}}`

## Repository layout

<!-- BACKEND_ROOT_BEGIN -->
- **Backend**: `{{BACKEND_ROOT}}`
<!-- BACKEND_ROOT_END -->
<!-- FRONTEND_ROOT_BEGIN -->
- **Frontend**: `{{FRONTEND_ROOT}}`
<!-- FRONTEND_ROOT_END -->
- **Specs / standards**: `{{SPECS_ROOT}}`
- **Integration state**: `{{INTEGRATION_STATE_PATH}}`

## Operating contract

This framework is **engine-obligatory** for three work classes:

1. **Ticket lifecycle** — every code change goes through `enrich-us → plan → develop → verify → commit → update-docs`. Each step has a mechanical state-machine gate; bypassing is prohibited.
2. **Strategy design** — architectural decisions go through the LangGraph strategy engine (bounded debate + critic + human gate).
3. **Audits** — `/audit` runs against the product code with structured phase reports.

**Read-only Q&A** about the codebase is native — no framework ceremony required.

**Pure-prompt / freestyle execution of those three classes is PROHIBITED.** The agent is the engine's executor: it supplies cognition at the engine's interrupts and pauses at the human gates.

Entrypoint:

```bash
python3 -m framework.cli.run --mode <lifecycle|strategy> --ticket <TICKET-ID> --module <module> --work-impl claude --advance
```

The engine exits with:
- `0` → done.
- `10` → a `WorkRequest` (cognitive work): do it with native tools, then `--resume --feed <fields.json>`.
- `20` → a human / gate stop: surface it to the operator and wait.

## Standards references

- **Workflow standards**: `{{WORKFLOW_STANDARDS}}`
- **Audit standards**: `{{AUDIT_STANDARDS}}`
<!-- DATA_MODEL_BEGIN -->
- **Data model**: `{{DATA_MODEL_PATH}}`
<!-- DATA_MODEL_END -->
<!-- API_SPEC_BEGIN -->
- **API spec**: `{{API_SPEC_PATH}}`
<!-- API_SPEC_END -->

## Key rules (must-follow)

- **Never bypass the lifecycle.** Touch code via `/lifecycle <TICKET>` only.
- **Never bypass push gates.** Direct merges to `main` require the operator's explicit authorization phrase.
- **Never cite pre-squash SHAs** in lifecycle artifacts (plans / records / specs). Use merge-commit SHAs (reachable from `main`) or narrative paraphrase.
- **English-only** in code, commits, and lifecycle artifacts.
- **Workspace contract**: when this repo's `forge.config.yml` declares `<workspace.*>` symbolic refs, treat them as the canonical references in lifecycle interrupts — the LangGraph engine injects the workspace dict into every `WorkRequest` context.

## Sources of truth

- `forge.config.yml` — workspace contract (this file's source).
- `CLAUDE.md` — full Claude Code operating contract.
- `.github/copilot-instructions.md` — GitHub Copilot Workspace mirror.
- `ai-bootstrap.md` — compact bootstrap for chat-web AI (paste as first prompt).

For the full operating contract (Proactive Interception filter, engine internals, push-gate phrasing), read `CLAUDE.md`.
