> **Auto-generated by `em-cli render-gateway` from `forge.config.yml` — DO NOT EDIT BY HAND.**
> Compact fallback for chat-web AIs (ChatGPT / Gemini / Claude.ai web) without filesystem access. Paste as the first prompt.

# ai-bootstrap — {{PRODUCT_NAME}}

I work on **{{PRODUCT_NAME}}**, a `{{STACK_LANGUAGE}}` / `{{STACK_FRAMEWORK}}` project (`{{STACK_VERSION}}`) governed by the **em-development-framework** — a 6-step ticket lifecycle (`enrich-us → plan → develop → verify → commit → update-docs`) backed by a LangGraph engine. The framework is engine-obligatory for three work-classes (lifecycle, strategy, audits); read-only Q&A about the code is native.

**Repo layout**:
<!-- BACKEND_ROOT_BEGIN -->
- Backend: `{{BACKEND_ROOT}}`
<!-- BACKEND_ROOT_END -->
<!-- FRONTEND_ROOT_BEGIN -->
- Frontend: `{{FRONTEND_ROOT}}`
<!-- FRONTEND_ROOT_END -->
- Specs / standards: `{{SPECS_ROOT}}` (workflow: `{{WORKFLOW_STANDARDS}}`, audit: `{{AUDIT_STANDARDS}}`)

**Key rules**:
- Code changes go through the lifecycle. No freestyle implementations.
- Cite merge-commit SHAs only (never pre-squash SHAs in lifecycle artifacts).
- English-only in code + commits + lifecycle artifacts.
- Push to `main` requires the operator's exact authorization phrase.

**For full context**:
- `AGENTS.md` — universal gateway (Cursor / Codex / Claude Code / Copilot / Windsurf).
- `CLAUDE.md` — full Claude Code operating contract (Proactive Interception filter, engine entrypoint, push-gate phrasing).
- `.github/copilot-instructions.md` — GitHub Copilot Workspace mirror.

If you can read files, start with `AGENTS.md` or `CLAUDE.md`. If you can't (chat-web sandbox), the above is the minimum context to be useful without driving framework-obligatory work blind.
